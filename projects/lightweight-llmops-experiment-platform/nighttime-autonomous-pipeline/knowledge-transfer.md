# Knowledge Transfer

## Intra-cycle transfer

Failure analysis from iteration N informs prompt revision in iteration N+1.

## Cross-cycle transfer

Reports, prompt revisions, parameter settings, evaluation outcomes, and decisions are retained after each cycle. Later cycles load this history as a historical learning layer.

This prevents each run from starting without prior context.
