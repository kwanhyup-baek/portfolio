# Workflow

```text
Prompt → Batched evaluation → Human review → Prompt revision → Re-evaluation
```

Human review determines whether a failure requires a local correction or a change in prompt direction. The best prompt from this loop becomes the input to overnight experimentation.
